PORT=3002
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Required: long random string for signing JWT access tokens
JWT_SECRET=a9b92ebb498545cf2653a2da8b142ecd8f8759685b0521097b79c67bcda858e2

# Optional: access token lifetime (jsonwebtoken format, e.g. 15m, 1h, 7d). Default: 7d
# JWT_EXPIRES_IN=7d

# Refresh tokens are not implemented yet — no REFRESH_TOKEN_SECRET.

# Comma-separated user UUIDs allowed to call GET/PATCH /api/admin/*
# ADMIN_USER_IDS=uuid-one,uuid-two
