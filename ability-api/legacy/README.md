This folder contains the **previous** Prisma-based wallet/ledger implementation (middleware, routes, services, types). The running server is only **`../src/index.ts`** (Express + Neon + Clerk).

You can delete this folder once you no longer need the old code for reference.
